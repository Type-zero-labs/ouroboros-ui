//! Typography — font registration and role-based font helpers.
//!
//! Registers the bundled faces (Geist Sans, Victor Mono) plus Phosphor icons (via
//! `egui-phosphor`), and exposes `font_h1`/`h2`/`heading`/`body`/`small`/`mono` reading
//! sizes from [`core`](crate::tokens::core). Populated in T05.
